# v0.2.3 Pre-Evaluation Freeze

## 日本語概要

この文書は、VisA `pcb2`のmethod fit前に、push済みsource commit、固定configとartifact schema、正常参照20枚、正常校正881枚、opaque final-test 200枚、3方式の順序、8個のhard-gate順をSHA-256付きで固定した記録です。凍結時点で画像decode、fit、threshold calibration、scoring、label reveal、評価は未実施で、raw dataと保護情報はGitに含めていません。次の許可済み段階は`v0.2.4`のreference fittingとnormal-only calibrationです。詳細は以下の英語本文に記載します。

---

## English Summary

The v0.2.3 checkpoint binds one pushed source commit to the exact evaluation contract, normal-only partitions, opaque final-test boundary, method order, and hard-gate order before any method fitting. The record contains no raw data, semantic final-test path, label, HMAC key, or machine-local path.

## Status

**Complete. The evaluation boundary remains unrevealed and no fitting or calibration has begun.**

The freeze was created only after source commit `462738321da403ce1f77ef23b0cb823008c32694` matched local `HEAD` and `origin/main`, the worktree was clean, and the commit's CI and Quick Start workflows had succeeded.

## Frozen Artifact

The committed artifact is [`pre-evaluation-freeze.json`](../artifacts/v0.2/evaluation/visa-pcb2-v0-2-final/freeze/pre-evaluation-freeze.json), with SHA-256:

```text
ae552d805dd9648163a48683bad828c7e1b7ecc4f1d69f1fa28511363b08ce3b
```

It validates as `pre_evaluation_freeze` under the unchanged [`evaluation-artifacts/v0.2`](../schemas/v0.2/evaluation-artifacts.json) contract. The `v0.2.3` milestone label is progress metadata; it does not change the parent protocol, run ID, config, schema, method, metric, or decision rule.

## Frozen Identities

| Item | Frozen value |
| --- | --- |
| Run ID | `visa-pcb2-v0-2-final` |
| Source commit | `462738321da403ce1f77ef23b0cb823008c32694` |
| Evaluation config SHA-256 | `9ea3a7156aeb3c6efc87c8ae3811444421bd3e86ea25b7ac014893c7e5892265` |
| Artifact schema SHA-256 | `4178d24f7210f8f859f6c99386204022ac3b2e4ffe4b7b63cdfea00d0c79f31d` |
| Preregistration commit | `b873bacc4f677a4c82f3944c09a7374037cb7c50` |
| Preregistration document SHA-256 | `6306c2122f69aa96dcfe1f377518e7c6795a096eceb085be5b829262b55482b9` |
| Reference manifest SHA-256 | `e587f1808262480261ae8a7b940faff0d9ef5f83cf215028b31490ba48369b99` |
| Calibration manifest SHA-256 | `77d5adb588e7d463e7fcab1c10b841b9ad23d827b51138c31f42dac35bd99ca3` |
| Opaque scoring-manifest SHA-256 | `32ea52ed1b9872f39ae27f5d58a353ea84b8b143642e3a7f0fabe940184705e8` |
| Sealed-mapping SHA-256 | `0e40e3777e797fd5099fd7ea8fd307547e396785dc33219f0bc0b3120e3dfe39` |
| Label reveal complete | `false` |

The source commit fixes the tracked root and isolated dependency lock files, implementation, and validation code. The config separately records the DINOv2 source revision, source-archive hash, checkpoint hash, isolated-environment lock hash, fixed 224-pixel CPU method, classical-method parameter sources, partitions, metrics, timing rules, and hard gates. No new environment or method choice was introduced by this checkpoint.

## Fixed Method and Gate Order

Methods will be processed in this order:

1. `ecc_residual`
2. `patch_hog_ocsvm`
3. `dinov2_vits14_224_nn`

Hard gates will be applied in this order:

1. `method_fit`
2. `test_leakage`
3. `final_test_normal_fpr`
4. `final_test_anomaly_recall`
5. `cpu_p95_scoring_latency`
6. `normal_reference_count`
7. `anomaly_training_labels`
8. `reproducibility`

No weighted aggregate score, gate waiver, or post-result threshold change is permitted.

## Boundary Checks Performed

The checkpoint runner required all of the following before writing the artifact:

- the public evaluation tree contained only the earlier boundary record;
- the external boundary inventory exactly matched the prepared layout;
- the boundary state still asserted no image decode or display, fit, threshold calibration, anomaly scoring, label reveal, or raw data in Git;
- the reference manifest contained exactly 20 records and the calibration manifest was non-empty;
- the normal-manifest, opaque scoring-manifest, and sealed-mapping hashes matched the prepared boundary;
- the opaque scorer manifest contained only label-free asset records;
- the 32-byte HMAC ordering key remained a non-symlink file with mode `0600` outside Git;
- the public boundary record retained its exact committed SHA-256 and matched the external aggregate counts and identities; and
- the output path did not already exist.

The runner hashed the sealed mapping but did not parse its contents. It did not read a final-test label, semantic final-test path, or image byte.

## Counts and Protected Data

| Partition | Count | Access at freeze |
| --- | ---: | --- |
| Normal reference | `20` | Known-normal manifest identity only |
| Normal calibration | `881` | Known-normal manifest identity only |
| Final test | `200` | Opaque asset IDs and byte identities only |

Raw VisA bytes, normal manifests, final-test source paths, labels, HMAC key, and sealed mapping remain under ignored local storage. The public artifact contains their permitted aggregate identities only. No dataset image or mask was added to Git.

## Non-Overwrite and Change Rule

The creation command was:

```bash
uv run --locked --no-sync python scripts/create_v0_2_3_pre_evaluation_freeze.py \
  --source-commit 462738321da403ce1f77ef23b0cb823008c32694
```

The command refuses to overwrite an existing freeze. Recreating the file is neither required nor authorized for this fixed run. Any change to a frozen method, partition, threshold rule, metric, timing boundary, hard gate, or label boundary requires a new preregistration rather than an edit to this artifact.

## Claim Boundary

This checkpoint provides process evidence only. It does not provide accuracy, latency, calibration, reproducibility, failure-case, or adoption evidence. It does not show that DINOv2 or either classical method fits the `pcb2` reference set or passes any final decision gate.

## Next Authorized Stage

The next stage is **v0.2.4 — Reference Fitting and Normal-Only Calibration**. It may decode only the normal reference and calibration partitions needed by the fixed methods. Final-test assets, semantic paths, labels, and sealed mapping remain outside that stage. The fitted-state and threshold artifacts must be committed and pushed before label-free final-test scoring begins.

## License Boundary

The runner, tests, public freeze record, and this documentation are source-available under the PolyForm Noncommercial License 1.0.0. VisA remains separately licensed under CC BY 4.0 and is not included in Git. DINOv2 assets and third-party dependencies remain governed by their respective upstream licenses; the repository's PolyForm terms do not replace those licenses.
