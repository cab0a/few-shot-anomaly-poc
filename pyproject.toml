[project]
name = "few-shot-anomaly-poc"
version = "0.1.0"
description = "A preregistered CPU-only proof of concept for few-shot visual anomaly detection."
readme = "README.md"
requires-python = "==3.13.14"
dependencies = [
    "numpy==2.5.1",
    "opencv-python-headless==4.13.0.92",
    "scikit-image==0.26.0",
    "scikit-learn==1.9.0",
]

[tool.uv]
package = false
required-version = "==0.11.32"
